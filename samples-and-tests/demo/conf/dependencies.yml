# Application dependencies

require:
    - play
    - play -> paginate head

    - oracle -> oracle

repositories:
    - test-modules:
        type:       local
        artifact:   ${application.path}/../../../[module]
        contains:
            - oracle -> *
